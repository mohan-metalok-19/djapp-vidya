module.exports = {
    assets: ['./assests/fonts'], // Include other asset paths as needed
    dependencies: {
        // Add custom native module dependencies here if required
        ios: {},
        android: {}

    },
};